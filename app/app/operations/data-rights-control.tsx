"use client";
import {useState} from "react";
export type DataRightRequest={id:string;reference:string;request_type:string;status:string;request_summary:string;identity_method:string;identity_verified_at:string;retention_review:string;retention_reason:string|null;decision_summary:string|null;created_at:string;updated_at:string;patient_profiles:{full_name:string}|{full_name:string}[]|null};
export function DataRightsControl({initialRequests,canManage}:{initialRequests:DataRightRequest[];canManage:boolean}){const [items,setItems]=useState(initialRequests),[busy,setBusy]=useState(""),[message,setMessage]=useState(""),[error,setError]=useState("");async function update(item:DataRightRequest,status:string){let retentionReview=item.retention_review,retentionReason=item.retention_reason??undefined,decisionSummary=item.decision_summary??undefined;if(["approved","processing","completed"].includes(status)&&retentionReview==="pending"){retentionReview=window.prompt("Retention result: eligible, retain_partial, or retain_full","eligible")?.trim()??"";if(!["eligible","retain_partial","retain_full"].includes(retentionReview))return;retentionReason=window.prompt("Document the retention reason.")?.trim();if(!retentionReason)return}if(["completed","rejected"].includes(status)){decisionSummary=window.prompt("Document the final decision and action.")?.trim();if(!decisionSummary)return}setBusy(item.id);setError("");const response=await fetch("/api/operations/data-requests",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id:item.id,status,retentionReview,retentionReason,decisionSummary})});const body=await response.json();setBusy("");if(!response.ok){setError(body.error??"Request could not be updated.");return}setItems(c=>c.map(x=>x.id===item.id?body.request:x));setMessage(`${body.request.reference} moved to ${body.request.status}.`)}const active=items.filter(x=>!["completed","rejected","cancelled"].includes(x.status));return <section className="data-rights-control"><header><div><span className="app-eyebrow">PATIENT PRIVACY</span><h3>Data-rights request queue</h3><p>Verify, review and document each request. Erasure never runs automatically.</p></div><b>{active.length} active</b></header>{message&&<p className="incident-success">✓ {message}</p>}{error&&<p className="incident-error">{error}</p>}{active.length?<div>{active.map(item=>{const patient=Array.isArray(item.patient_profiles)?item.patient_profiles[0]:item.patient_profiles;return <article key={item.id}><div><span>{item.reference} · {item.request_type.replaceAll("_"," ")}</span><b>{patient?.full_name??"Patient"}</b><small>{item.request_summary}</small><em>Identity: {item.identity_method.replaceAll("_"," ")}</em></div><strong>{item.status.replaceAll("_"," ")}</strong>{canManage&&<nav>{item.status==="submitted"&&<button disabled={busy===item.id} onClick={()=>update(item,"identity_verified")}>Verify identity</button>}{item.status==="identity_verified"&&<button disabled={busy===item.id} onClick={()=>update(item,"under_review")}>Start review</button>}{item.status==="under_review"&&<button disabled={busy===item.id} onClick={()=>update(item,"approved")}>Approve</button>}{item.status==="approved"&&<button disabled={busy===item.id} onClick={()=>update(item,"processing")}>Process</button>}{item.status==="processing"&&<button disabled={busy===item.id} onClick={()=>update(item,"completed")}>Complete</button>}<button className="danger" disabled={busy===item.id} onClick={()=>update(item,"rejected")}>Reject</button></nav>}</article>})}</div>:<div className="operations-empty"><i>✓</i><b>No active data requests</b><span>New verified patient requests will appear here.</span></div>}</section>}
